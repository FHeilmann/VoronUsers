name: VoronUsers CI v2
run-name: VoronUsers CI version 2 🚀
on:
  pull_request:
    types: [opened, reopened, synchronize]
jobs:
  VoronUsers-PR:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - name: Get changed files and check whitespace 🔍
        id: changed-files
        uses: FHeilmann/pr-changed-files-check-whitespace@main
      - name: List all changed files 📃
        run: |
          echo "::group::changed-files"
          for file in ${{ steps.changed-files.outputs.changed_files }}; do
            echo "Changed file: $file"
          done
          echo "::endgroup::"
      - name: Perform Sparse Checkout 🛒
        id: sparse-checkout
        uses: FHeilmann/sparse-checkout@main
        with:
          files: ${{steps.changed-files.outputs.changed_files}}
          folders: ".workflow_files"
          patterns: ""
          path: ${{github.workspace}}
          branch: ${{github.ref}}
      - name: Install apt packages (cached) 🛠️
        uses: awalsh128/cache-apt-pkgs-action@v1.3.0
        with:
          packages: admesh, libadmesh-dev
          version: 1.0
      - name: Setup python 3.11 🐍
        id: python311
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Handle python cache 🧰
        uses: actions/cache@v3
        id: cache-python
        env:
          cache-name: cache-python
        with:
          path: ${{ env.pythonLocation }}
          key: ${{ runner.os }}-build-${{ env.cache-name }}
      - if: ${{ steps.cache-python.outputs.cache-hit != 'true' }}
        name: Install Python packages (cached) 🛠️
        run: |
          pip install pykwalify admesh git+https://github.com/ChristophSchranz/Tweaker-3.git
      - name: Show tool versions 🏃
        id: run-tools
        run: |
          admesh --version
          echo "Pykwalify $(pykwalify --version)"
          tweaker3 --version
      - name: Check STLs ✅
        id: check-stls
        run: |
          echo "::group::check-stls"
          chmod +x ${{ github.workspace }}/.workflow_files/validate-stl.py
          for file in $(echo $(find ${{ github.workspace }} -type f -iname '*.STL') | tr '\n' ' '); do
            python3 ${{ github.workspace }}/.workflow_files/validate-stl.py $file || touch failed
          done
          if [ -f "failed" ]; then
            echo "Error occurred while validating STLs."
            exit 255
          fi
          echo "::endgroup::"
      - name: Check Metadata files ✅
        id: check-metadata
        run: |
          echo "::group::check-metadata"
          for file in $(echo $(find ${{ github.workspace }} -type f -iname '.metadata.yml') | tr '\n' ' '); do
            echo "Checking metadata file $file.."
            pykwalify -d $file -s ${{ github.workspace }}/.workflow_files/yaml_schema.yml
          done
          echo "::endgroup::"
